using SOA.Base;
using UnityEditor;
using $USING$;

namespace SOA.Common.CustomTypes
{
    [CustomEditor(typeof($TYPENAME$GameEvent))]
    public class $TYPENAME$GameEventEditor : UnityEventSOEditor<$TYPENAME$GameEvent, $TYPENAME$UnityEvent, $TYPE$>
    {
    }
}