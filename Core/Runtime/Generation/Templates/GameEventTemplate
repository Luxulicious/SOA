using SOA.Base;
using UnityEngine;
using $USING$;

namespace SOA.Common.CustomTypes
{
    [CreateAssetMenu(fileName = "New $TYPENAME$ Event", menuName = "SOA/$TYPENAME$/Event", order = 1)]
    public class $TYPENAME$GameEvent : GameEvent<$TYPENAME$UnityEvent, $TYPE$>
    {
    }
}