using SOA.Base;
using UnityEngine;
using $USING$;

namespace SOA.Common.CustomTypes
{
    [CreateAssetMenu(fileName = "New $TYPENAME$ List", menuName = "SOA/$TYPENAME$/List", order = 1)]
    public class $TYPENAME$ReferenceListVariable : ReferenceListVariable<$TYPENAME$ReferenceList, $TYPENAME$Reference, $TYPENAME$Variable, $TYPE$,
        $TYPENAME$UnityEvent, $TYPENAME$$TYPENAME$UnityEvent, $TYPENAME$ReferenceUnityEvent>
    {
    }
}