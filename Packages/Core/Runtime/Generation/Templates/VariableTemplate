using SOA.Base;
using UnityEngine;
using $USING$;

namespace SOA.Common.CustomTypes
{
    [CreateAssetMenu(fileName = "New $TYPENAME$ Variable", menuName = "SOA/$TYPENAME$/Variable", order = 1)]
    public class $TYPENAME$Variable : Variable<$TYPE$, $TYPENAME$UnityEvent, $TYPENAME$$TYPENAME$UnityEvent>
    {
    }
}